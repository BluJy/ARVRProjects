using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RubberBandTest : MonoBehaviour
{
    /*
    // Start is called before the first frame update
    private Vector3 controllerPosition;
    private Trajectory coachTrajectory;
    [SerializeField] private LineRendererController line;



    void Start()
    {
        List<Vector3> tPoints = new List<Vector3>();
        tPoints.Add(new Vector3((float)-8.98766899, (float)0.428279281, (float)-1.99968338));
        tPoints.Add(new Vector3((float)-8.79683304, (float)0.433975697, (float)-4.47770309));
        tPoints.Add(new Vector3((float)-8.79683304, (float)0.433975577, (float)-6.85888004));
        tPoints.Add(new Vector3((float)-8.79683304, (float)2.28536963, (float)-8.71881962));

        controllerPosition = transform.position;
        coachTrajectory = new Trajectory((float)1.22, new Vector3((float)1, (float)2, (float)1), tPoints);

        RubberBand.updateRubberBand(line, coachTrajectory, controllerPosition, 1);


    }

    // Update is called once per frame
    void Update()
    {
        controllerPosition = transform.position;
        RubberBand.updateRubberBand(line, coachTrajectory, controllerPosition, 1);
        

    }
    */
}
